# Explorer 3 Context

Milestone 1 & 3: CI Pipeline, Report Quality, SARIF & JSON Output Assessment
Target: Investigate `covopt ci` subcommand implementation and execution flow, `covopt audit --json` stdout JSON formatting/validity, SARIF v2.1.0 generation, and acceptance criteria verification setup (`cargo check --workspace`, `cargo test --workspace`, `covopt ci`, `covopt audit --json`).
