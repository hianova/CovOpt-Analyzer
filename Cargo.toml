[workspace]
members = ["covopt-macro", "covopt_core", "covopt_cli"]
resolver = "2"

[profile.dev]
incremental = false
split-debuginfo = "unpacked"

[profile.dev.package."*"]
opt-level = 3
debug = false
