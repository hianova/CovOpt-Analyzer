[package]
name = "covopt-schema"
version = "2.0.0"
edition = "2024"
description = "Shared versioned source-contract and parameter metadata for CovOpt"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
